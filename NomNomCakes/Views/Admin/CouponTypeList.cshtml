@model IEnumerable<Models.CouponType>

@{
    ViewBag.Title = "Coupon Type List";
}

<h2>Coupon Type List</h2>

<p>
    @Html.ActionLink("Create New", "CouponTypeCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CouponModule)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CouponModule)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "CouponTypeEdit", new { id=item.CouponTypeId }) |
            @Html.ActionLink("Delete", "CouponTypeDelete", new { id = item.CouponTypeId })
        </td>
    </tr>
}

</table>
